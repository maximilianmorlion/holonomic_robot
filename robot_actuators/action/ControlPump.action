# Goal - Pump control command
int32 pump_id        # Pump identifier
bool enable          # true = on, false = off
float32 duty_cycle   # PWM duty cycle (0.0 to 1.0), only if variable speed
float32 duration_sec # Auto-shutoff after duration, 0 = manual control
---
# Result - Final state
bool success
bool is_running      # Current pump state
string message
---
# Feedback - Runtime updates
float32 elapsed_time  # Time since activation (seconds)
float32 pressure_pa   # Current pressure if sensor available
